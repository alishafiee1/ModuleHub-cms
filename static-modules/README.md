# static-modules (deprecated)

User-uploaded static ZIP modules were removed in `public-homepage-architecture-v2`.

Regular pages (gallery, markdown viewer) live in `core/builtin-modules/`.
Standalone modules with Docker live in `standalone-modules/`.

The former `sample-gallery` was migrated to `core/builtin-modules/sample-gallery/`.
